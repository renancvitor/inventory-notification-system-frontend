export interface ProductFormValue {
  productName: string;
  categoryId: number;
  price: number;
  validity: string;
  description: string;
  stock: number;
  minimumStock: number;
  brand: string;
}
